# Icons

Drop a 1024×1024 PNG at `icon.png` and run `npx @tauri-apps/cli icon icon.png`
to generate platform-specific icon variants. Tauri's build will fail
without these.

For the first ship, the EaseVerse orange gradient logo is recommended.
